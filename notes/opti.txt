# Do one ip -> print -> free
# -> rework main loop so that multiple hosts can be 
scanned together, especially when delay is applied
# Lighten struct ip 
# delete down hosts from the struct (in live??)
